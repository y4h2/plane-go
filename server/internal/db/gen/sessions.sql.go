// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: sessions.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const createSession = `-- name: CreateSession :one
insert into sessions (key, user_id, expires_at)
values ($1, $2, $3)
returning key, user_id, created_at, expires_at
`

type CreateSessionParams struct {
	Key       string    `json:"key"`
	UserID    uuid.UUID `json:"user_id"`
	ExpiresAt time.Time `json:"expires_at"`
}

func (q *Queries) CreateSession(ctx context.Context, arg CreateSessionParams) (Session, error) {
	row := q.db.QueryRow(ctx, createSession, arg.Key, arg.UserID, arg.ExpiresAt)
	var i Session
	err := row.Scan(
		&i.Key,
		&i.UserID,
		&i.CreatedAt,
		&i.ExpiresAt,
	)
	return i, err
}

const deleteSession = `-- name: DeleteSession :exec
delete from sessions where key = $1
`

func (q *Queries) DeleteSession(ctx context.Context, key string) error {
	_, err := q.db.Exec(ctx, deleteSession, key)
	return err
}

const getSessionUser = `-- name: GetSessionUser :one
select u.id, u.email, u.password, u.first_name, u.last_name, u.display_name, u.avatar, u.is_active, u.is_bot, u.date_joined, u.last_login, u.created_at, u.updated_at
from sessions s
join users u on u.id = s.user_id
where s.key = $1 and s.expires_at > now()
`

func (q *Queries) GetSessionUser(ctx context.Context, key string) (User, error) {
	row := q.db.QueryRow(ctx, getSessionUser, key)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.Password,
		&i.FirstName,
		&i.LastName,
		&i.DisplayName,
		&i.Avatar,
		&i.IsActive,
		&i.IsBot,
		&i.DateJoined,
		&i.LastLogin,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
